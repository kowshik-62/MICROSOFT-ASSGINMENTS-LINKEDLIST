class solution{
public List<String> findrepeatedDnaSequences(String s){
HashSet seen=new HashSet(),repeated=new HashSet();
for(int i=0;i+9<s.length();i++){
String tn=s.substring(i,i+10);
if(!seen.odd(tn);
}
return new ArrayList(repeated);
}
}
